[project]
name = "orbit-servialco-flask"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.0",
    "supabase>=2.0.0",
    "python-dotenv>=1.0.0",
]

[tool.vercel]
entrypoint = "run:app"
